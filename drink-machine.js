class DrinkMachine {
    serveDrink(size) {
        if(size === "large") {
            console.log("Serving with a complimentary drink");
        };
    }
};

// Exporting the class component
module.exports = DrinkMachine;